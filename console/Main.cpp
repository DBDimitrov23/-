#include "BookingSystem.h"

// Функцията main е входната точка на всяка C++ програма
int main() {
    BookingSystem system; // създаваме обект от главния клас (зарежда се демо данни автоматично)
    system.run();           // стартираме интерактивното конзолно меню
    return 0;                // връщаме 0, което означава, че програмата е завършила успешно
}